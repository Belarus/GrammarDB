<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.github.belarus</groupId>
		<artifactId>linguistics.grammardb</artifactId>
		<version>1.0.1</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>linguistics.grammardb.data.cache</artifactId>
	<version>1.1.0</version>
	<name>GrammarDB as in-memory cache</name>

	<properties>
		<maven.build.timestamp.format>yyyyMM</maven.build.timestamp.format>
	</properties>

	<build>
		<finalName>RELEASE-${maven.build.timestamp}</finalName>
		<plugins>
			<plugin>
				<groupId>io.github.belarus</groupId>
				<artifactId>linguistics.grammardb.compiler</artifactId>
				<version>1.0.6</version>
				<executions>
					<execution>
						<id>xml2xml</id>
						<goals>
							<goal>export-database</goal>
						</goals>
						<configuration>
							<xmlDataDir>data</xmlDataDir>
							<exportDir>${project.build.directory}/export/</exportDir>
							<reviewDir>${project.build.directory}/to-review-no-show/</reviewDir>
						</configuration>
					</execution>
					<execution>
						<id>xml2text</id>
						<goals>
							<goal>export-spell-checker</goal>
						</goals>
						<configuration>
							<xmlDataDir>data</xmlDataDir>
							<textOutputFile>${project.build.directory}/export/spellchecker/slovy-2008.txt</textOutputFile>
							<textOutputFileWithStress>${project.build.directory}/export/spellchecker/slovy-2008-stress.txt</textOutputFileWithStress>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.3.1</version>
				<executions>
					<execution>
						<id>copy-generated-resources</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.outputDirectory}</outputDirectory>
							<resources>
								<resource>
									<directory>${project.build.directory}/export/binary-cache/</directory>
									<includes>
										<include>**/*</include>
									</includes>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<appendAssemblyId>false</appendAssemblyId>
							<attach>false</attach>
							<descriptors>
								<descriptor>${project.basedir}/data-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
